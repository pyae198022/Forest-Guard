# ForestGuard — FastAPI backend runtime (Render / any Python host).
# Versions are pinned to the environment the app was validated against.
fastapi==0.141.1
uvicorn==0.52.4
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.1
joblib==1.6.0
openpyxl==3.1.5
python-multipart==0.0.32